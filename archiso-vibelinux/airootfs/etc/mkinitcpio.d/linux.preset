# /etc/mkinitcpio.d/linux.preset for VibeLinux

# mkinitcpio preset file for the kernel
ALL_config="/etc/mkinitcpio.conf"
ALL_kver="/boot/vmlinuz-linux"

PRESETS=('default')

default_image="/boot/initramfs-linux.img"
default_options=()
